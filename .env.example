# Base URL of the deployed AWS API Gateway stage.
# Example: https://abc123xyz.execute-api.ap-south-1.amazonaws.com
# Never put secrets or AWS credentials in a VITE_* variable — this value
# is bundled into the client build and is publicly visible.
VITE_API_URL=https://YOUR_API_ID.execute-api.ap-south-1.amazonaws.com